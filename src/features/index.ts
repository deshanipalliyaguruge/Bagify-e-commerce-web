/**
 * Feature directories scaffold.
 *
 * Each feature follows this internal structure:
 *
 * features/
 * └── [feature-name]/
 *     ├── components/     — React components scoped to this feature
 *     ├── actions/        — Next.js server actions (mutations)
 *     ├── hooks/          — Client hooks for this feature
 *     ├── types/          — TypeScript types/interfaces
 *     └── schemas/        — Zod validation schemas
 *
 * Current features (stubs, implemented incrementally):
 * - auth          — Registration, login, session management
 * - products      — Listing, filtering, detail pages
 * - cart          — Cart state and item management
 * - checkout      — Checkout flow and order placement
 * - orders        — Order history and status tracking
 * - account       — User profile management
 * - search        — Search and filter logic
 * - wishlist      — Wishlist management
 */

export {}
